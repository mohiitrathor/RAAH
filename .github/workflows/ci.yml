name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Test & Verify
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Validate frontend JavaScript syntax
        run: |
          find frontend/js -name "*.js" -exec node --check {} +

      - name: Run automated test suite
        run: |
          ./run.sh test

      - name: Execute end-to-end headless demonstration
        run: |
          ./run.sh demo

      - name: Run system performance benchmarks (informational)
        run: |
          ./run.sh benchmark || echo "Benchmark completed with warnings on shared runner"
